import style from './OrderView.module.scss';

const OrderView = () => {
  return (

    <div
      style={{
        height: '100vh',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        fontSize: 40,
        color: '#010101',
      }}
    ><h1>Order page</h1>

    </div>
  );
};
export default OrderView;
